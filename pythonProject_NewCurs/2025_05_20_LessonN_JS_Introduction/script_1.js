            //let ключевое слово для объявления переменной.
            let number = 223; //Числовой тип данных
            let message = "Hello User!"; //Строковый тип данных
            let userName = "Bob";
            let isValid = true; //false; Логический тип данных
            const pi = 3.14;
            number = 500; // Переменной numrer присвоил новое значение

            alert("Hello World!");
            alert(message); //stateman комментарий, alert() - функция (выполняет некоторое действие, набор действий или событий)
            alert(number);
            alert(isValid);
            alert(pi);